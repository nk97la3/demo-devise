<%= form_for @product, method: :put, url: wizard_path do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
        <% @product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p hidden="hidden">
    <%= f.text_field :user_id , require: true, value: current_user.id %></p>
  <p>
  <div class="field"> 
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
   <div class="field">
    <%= f.label :opening %><br>
    <%= f.text_area :opening %>
  </div>
  <div class="actions">
    <%= f.submit "Next Step" %>
  </div>
  <%=f.submit"Save", :name =>"Save"%>
<% end %>
