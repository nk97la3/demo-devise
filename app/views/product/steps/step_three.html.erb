<%= form_for @product, method: :put, url: wizard_path do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
        <% @product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
    <%= f.label :conclusion %><br>
    <%= f.text_area :conclusion %>
  </div>
   <div class="field">
    <%= f.label :aboutauthor %><br>
    <%= f.text_area :aboutauthor %>
  </div>
  <div class="actions">
     <%= f.submit "Back"  , :name=>"Back"%><%= f.submit "Publish", :name=> "Publish" %>
  </div>
  <%=f.submit"Save", :name =>"Save"%>

<% end %>
